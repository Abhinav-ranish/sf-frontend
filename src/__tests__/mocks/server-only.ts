/**
 * Stub for the `server-only` package.
 *
 * Its real entry point throws unless the bundler resolves the `react-server`
 * condition, which Jest does not. `jest.config.ts` maps the import here so
 * server modules can be unit-tested directly.
 */
export {};
